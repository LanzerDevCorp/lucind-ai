# lucind-ai working state, in whatever project this runs against
.lucind/

# local agent state
.codegraph/
.atl/
*skill-registry*

# os
Thumbs.db
.DS_Store

# agy CLI scratch state
.antigravitycli/

# SQLite sidecars: transient, recreated on open, never part of a .backup snapshot
*.db-shm
*.db-wal

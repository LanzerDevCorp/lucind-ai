# Implement the packet compiler

## Done criteria
- The compiler preserves manual packet bytes.
- Create a commit containing the implementation.

## Return
Write the result envelope to `.lucind/result.json` in this worktree.
Validate it against `.lucind/result.schema.json` before writing.
